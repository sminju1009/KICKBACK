package org.example.businessserver.message;


import java.io.IOException;

public class MessageUnPacker {
//    public static void unpacking(byte[] in) throws IOException {
//        // try-with-resources 구문을 사용하여 자동으로 unPacker를 닫습니다.
//        try (MessageUnpacker unPacker = MessagePack.newDefaultUnpacker(in)) {
//            while (unPacker.hasNext()) {
//                // unpackString() 호출 시 발생할 수 있는 IOException을 처리합니다.
//                System.out.println(unPacker.unpackValue());
//            }
//        } catch (IOException e) {
//            System.err.println("언패킹 중 오류 발생: " + e.getMessage());
//            // 필요한 경우, 여기서 추가적인 예외 처리를 수행할 수 있습니다.
//            throw e; // 예외를 다시 던져 상위 호출자에게 알립니다.
//        }
//        System.out.println("done");
//    }
}
